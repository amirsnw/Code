import { ElementRef, OnDestroy, OnInit } from '@angular/core';
import { TaminLazyLoadService } from '../../../services/tamin-lazy-load/tamin-lazy-load.service';
import { DomSanitizer } from '@angular/platform-browser';
export declare class PdfViewerComponent implements OnInit, OnDestroy {
    private taminLazyLoadService;
    private domSanitizer;
    iframe: ElementRef;
    width: string;
    height: string;
    private baseUrl;
    private subscription;
    constructor(taminLazyLoadService: TaminLazyLoadService, domSanitizer: DomSanitizer);
    ngOnDestroy(): void;
    ngOnInit(): void;
    based64toBlob(b64Data: any, contentType: any, sliceSize?: number): Blob;
    openBased64(data: any): void;
    open(url: string): void;
}
