import { OnInit } from '@angular/core';
import { Router } from '@angular/router';
export declare class TaminSiteFooterComponent implements OnInit {
    private router;
    constructor(router: Router);
    ngOnInit(): void;
    redirectTo(destination: any): void;
}
