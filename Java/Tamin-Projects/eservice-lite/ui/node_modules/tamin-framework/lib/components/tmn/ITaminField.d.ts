import { AbstractControl } from '@angular/forms';
import { ElementRef, EventEmitter } from '@angular/core';
export interface ITaminField {
    value: string;
    control: AbstractControl;
    maxLength: Number;
    formControlName: string;
    placeHolder: string;
    blur: EventEmitter<any>;
    element: ElementRef;
}
