import { OnInit } from '@angular/core';
import { Router } from '@angular/router';
export declare class TaminSiteBottomMenuComponent implements OnInit {
    private router;
    constructor(router: Router);
    ngOnInit(): void;
    handleMenu(item: any): void;
}
