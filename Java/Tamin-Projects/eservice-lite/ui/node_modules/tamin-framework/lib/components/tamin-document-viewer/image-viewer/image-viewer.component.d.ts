import { ElementRef, OnDestroy, OnInit } from '@angular/core';
import { TaminLazyLoadService } from '../../../services/tamin-lazy-load/tamin-lazy-load.service';
export declare class ImageViewerComponent implements OnInit, OnDestroy {
    private taminLazyLoadService;
    img: ElementRef;
    width: string;
    height: string;
    private subscription;
    constructor(taminLazyLoadService: TaminLazyLoadService);
    ngOnInit(): void;
    open(url: string): void;
    ngOnDestroy(): void;
}
