import { OnInit } from '@angular/core';
import { TaminLazyLoadService } from '../../services/tamin-lazy-load/tamin-lazy-load.service';
export declare class TaminMapComponent implements OnInit {
    private taminLazyLoadService;
    js: string;
    css: string;
    width: string;
    height: string;
    id: any;
    constructor(taminLazyLoadService: TaminLazyLoadService);
    ngOnInit(): void;
    load(): Promise<any>;
    setLocation(latitude: number, longitude: number, zoom: number): void;
}
