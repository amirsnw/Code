import { ITaminApplicationConfig } from '../interfaces/tamin-application-config';
export declare class RequestCacheService {
    private taminApplicationConfig;
    private cache;
    constructor(taminApplicationConfig: ITaminApplicationConfig);
    isCacheable(url: string): boolean;
    hasCached(url: string): boolean;
    getCached(url: string): any;
    setCache(url: string, data: any): Map<any, any>;
    deleteCache(url: string): void;
}
